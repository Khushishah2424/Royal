const product = ["ihphone","ipad","mac"]

function diplayDrop(e){
    console.log(e)
    const data = e.target.getAttribute('data-value')
    if(data === "product"){
        var ul = document.createAttribute('ul');
        var div = document.createAttribute("")
    }
}